{
  "//": "IDE-facing config so editors (and the vitest oxc transform) resolve Node types for the core. Extends the backend tsconfig that globs this dir.",
  "extends": "../../tsconfig.json",
  "include": ["."],
  "exclude": ["../../node_modules"]
}
